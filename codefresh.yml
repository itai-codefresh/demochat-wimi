version: '1.0'
steps:
  build_step:
    title: Build
    type: build
    dockerfile: Dockerfile
    image_name: containers101/demochat
    tag: master
    
  unit_tests:
    title: Unit Tests
    image: alpine:latest
    fail_fast: false
    commands:
      - cf_export SOME=VALUE1
         
  unit_testss:
    title: Unit Tests
    image: ${{MY_IMAGE}}
    fail_fast: false
    commands:
      - env
    
  #launch_composition:
    #title: Launch Composition
    #type: launch-composition
    #composition: docker-compose.yml
    #environment_name: 'staging'
    #entry_point: demochat
